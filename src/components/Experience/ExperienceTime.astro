---
const { startDate, current, endDate, timeAge } = Astro.props
---

<time
  class='block mb-2 text-sm font-normal leading-none text-gray-400 dark:text-gray-500'
>
  {startDate} -{' '}
  {current ? 'Present' : `${endDate} · ${timeAge}`}
</time>
