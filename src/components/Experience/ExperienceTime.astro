---
const { startDate, current, endDate, timeAge } = Astro.props
---

<time
  class='mb-2 block text-sm font-semibold leading-none text-cod-gray-400 dark:text-gray-500'
>
  {startDate} -{' '}
  {current ? 'Present' : `${endDate} · ${timeAge}`}
</time>
